//a card that will have all of the elements for a person card in one. 

const PersonCard = ({person}) =>{

    return(
        <>
        {person.name}
        </>
    )

}

export default PersonCard;