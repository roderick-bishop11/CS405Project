//this is the main view. we want to abstract it out a bit from App.js, let's keep that clean!

import React  from "react";
const BirthdayView = () =>{
	return(
	// <Carousel> for every person
		<></>
	);
};

export default BirthdayView;